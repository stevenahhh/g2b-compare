{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimum permissions for the bundled main window",
  "windows": ["main"],
  "platforms": ["windows"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart"
  ]
}
